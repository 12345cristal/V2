# ================================================================
# CONFIGURACIÓN DE GOOGLE CALENDAR - ARCHIVO .env
# ================================================================
# Copiar estas líneas en el archivo backend/.env

# === GOOGLE CALENDAR API ===
# Ruta al archivo JSON de credenciales del Service Account
GOOGLE_CALENDAR_CREDENTIALS=credentials/google-calendar-service-account.json

# ID del calendario de Google (usar 'primary' para el calendario principal)
# O el ID específico del calendario compartido
GOOGLE_CALENDAR_ID=primary

# === EJEMPLO DE CALENDARIO COMPARTIDO ===
# Si usas un calendario específico, el ID se ve así:
# GOOGLE_CALENDAR_ID=abc123xyz456@group.calendar.google.com

# === ZONA HORARIA ===
# Ajustar en google_calendar_service.py según tu ubicación
# México (Sinaloa): America/Hermosillo (GMT-7)
# México (CDMX): America/Mexico_City (GMT-6)
